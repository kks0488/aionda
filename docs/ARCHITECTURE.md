# System Architecture

## Mission

> **"한국 AI 커뮤니티의 집단지성을 전문적인 콘텐츠로 정제하여 세계에 전파한다"**

**중요: 우리는 갤러리 글을 그대로 복사하는 것이 아닙니다.**

핵심 원칙: `품질 > 양` | `검증 > 속도` | `가치 > 조회수`

---

## Overview

AI온다 (aionda) 블로그는 DC Inside "특이점이 온다" 갤러리의 AI 관련 콘텐츠를 **큐레이션, 검증, 재구성**하여 글로벌 발행하는 완전 자동화 시스템입니다.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          AI온다 AUTOMATED PIPELINE                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  GitHub Actions (하루 4회: 02:00, 08:00, 14:00, 20:00 KST)                    │
│                                                                              │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐  │
│  │ Crawler  │──▶│Auto-Select│──▶│ Verifier │──▶│Translator│──▶│Publisher │  │
│  │(5 pages) │   │(score≥30)│   │ (Gemini) │   │ (Gemini) │   │  (MDX)   │  │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘  │
│       │              │              │              │              │          │
│       ▼              ▼              ▼              ▼              ▼          │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐  │
│  │data/raw/ │   │data/sel. │   │data/ver. │   │  +영어   │   │content/  │  │
│  │(440+)    │   │  (max 3) │   │(검증완료)│   │  번역    │   │posts/    │  │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                        Image Generation (Gemini)                      │   │
│  │  ┌─────────────────────────────────────────────────────────────┐    │   │
│  │  │  글 제목/태그 기반 → AI 이미지 생성 → public/images/posts/  │    │   │
│  │  └─────────────────────────────────────────────────────────────┘    │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Next.js 14 Blog (apps/web)                        │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐   │   │
│  │  │   /ko   │  │   /en   │  │  /api   │  │   OG    │  │ Vercel  │   │   │
│  │  │  pages  │  │  pages  │  │  routes │  │  Image  │  │  Deploy │   │   │
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘  └─────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Automation Strategy

### Daily Target: 3-5 Quality Posts

**양보다 질**: 품질 검증을 통과한 글만 발행

| 시간 (KST) | 실행 | 최대 글 수 |
|-----------|------|----------|
| 02:00 | 1회차 | 1-2개 |
| 08:00 | 2회차 | 1-2개 |
| 14:00 | 3회차 | 1-2개 |
| 20:00 | 4회차 | 1-2개 |
| **합계** | **4회** | **3-5개** (품질 유지) |

### Key Features

- **완전 자동화**: GitHub Actions cron 기반, 사람 개입 없이 24시간 운영
- **품질 게이팅**: 다단계 필터링 (조회수/좋아요 → 품질점수 → 검증점수)
- **에러 복구**: 각 단계 3회 재시도, 타임아웃 자동 복구
- **이미지 필수**: Gemini AI로 커버 이미지 자동 생성
- **중복 방지**: 제목 유사도 70% 이상 skip, work-queue 관리

## Components

### 1. Crawler (`packages/crawler` + `scripts/crawl.ts`)

DC Inside 갤러리에서 글을 수집합니다.

```
packages/crawler/
├── src/
│   ├── fetcher.ts     # HTTP 요청 (axios, 1초 딜레이)
│   ├── parser.ts      # HTML 파싱 (Cheerio)
│   └── index.ts       # 메인 exports
scripts/
├── crawl.ts           # 갤러리 크롤링 CLI
└── crawl-official.ts  # 공식 AI 소스 확인
```

**수집 데이터:**
```json
{
  "id": "930644",
  "title": "OpenAI GPT-5 출시 임박",
  "category": "정보/뉴스",
  "author": "nickname",
  "date": "2026.01.10",
  "views": 1234,
  "likes": 56,
  "content": "<html>...",
  "contentText": "plain text..."
}
```

### 2. Auto-Selector (`scripts/auto-select.ts`)

품질 점수 기반 자동 선별 시스템입니다.

**품질 점수 계산:**
```
기본 점수:
  + 조회수/50 (최대 20점)
  + 좋아요×2 (최대 20점)

보너스:
  + AI 키워드 (ai, gpt, claude 등): +5점/개
  + 정보/뉴스 카테고리: +10점
  + 콘텐츠 200자 이상: +10점

페널티:
  - 스팸 키워드 (광고, 코인 등): -15점/개
  - 제목 10자 미만: -20점
```

**필수 조건 (먼저 체크):**
```
✗ 제목이 "제목 없음", "무제", "ㅇㅇ" 등 → 즉시 거부
✗ 콘텐츠 500자 미만 → 즉시 거부
✗ 유의미한 문장 2개 미만 → 즉시 거부
```

**선택 기준:**
- `MIN_QUALITY_SCORE`: 30점 이상
- `MAX_POSTS`: 실행당 최대 3개
- 중복 제외: work-queue 체크, 유사도 70% 이상 skip

### 3. Verifier (`scripts/verify.ts`)

AI 기반 사실 검증 모듈입니다.

**검증 프로세스:**
```
1. 클레임 추출 (Gemini)
   └─ 글에서 검증 가능한 주장 추출

2. No-Claims 처리 (중요!)
   └─ 검증할 주장 없음 = verified: false, confidence: 0.3
   └─ 의견/잡담으로 간주, 저신뢰도 처리

3. 웹 검색 (Google Search)
   └─ 각 주장에 대해 출처 검색

4. 소스 티어 분류
   ├─ S: 공식 블로그 (openai.com, anthropic.com)
   ├─ A: 주요 언론 (reuters, techcrunch)
   ├─ B: 기술 블로그 (medium, dev.to)
   └─ C: 기타

5. 검증 점수 계산
   └─ verificationScore: 0.0 ~ 1.0
   └─ 발행 기준: 0.5 이상
```

**출력 예시:**
```json
{
  "postId": "930644",
  "claims": [
    {
      "text": "GPT-5가 2026년 1월 출시됨",
      "verified": true,
      "sources": ["openai.com/blog/gpt-5-launch"],
      "confidence": 0.95
    }
  ],
  "overallScore": 0.85,
  "recommendation": "publish"
}
```

### 4. Translator (`scripts/translate.ts`)

한국어 → 영어 번역 파이프라인입니다.

**특징:**
- 기술 용어 일관성 유지
- 컨텍스트 인식 번역
- 코드 블록, 링크 보존
- Markdown 포맷 유지

### 5. Image Generator (`scripts/generate-image.ts`)

Gemini AI로 커버 이미지를 자동 생성합니다.

**프로세스:**
```
1. 이미지 없는 포스트 탐색
2. 제목/태그 기반 프롬프트 생성
3. Gemini gemini-3-pro-image-preview 모델로 이미지 생성
4. public/images/posts/{slug}.{ext} 저장
5. MDX frontmatter에 coverImage 필드 추가
```

**스타일:**
- 모던, 미니멀 테크 스타일
- 다크 그라디언트 배경
- AI/기술 추상화 시각화
- 16:9 비율
- 텍스트/로고/얼굴 없음

### 6. Blog Site (`apps/web`)

Next.js 14 App Router 기반 블로그입니다.

```
apps/web/
├── app/
│   ├── [locale]/
│   │   ├── layout.tsx
│   │   ├── page.tsx           # 포스트 목록
│   │   └── posts/
│   │       └── [slug]/
│   │           └── page.tsx   # 포스트 상세
│   ├── api/
│   │   ├── og/route.tsx       # OG 이미지 생성
│   │   └── og-default/route.tsx
│   └── layout.tsx
├── content/
│   └── posts/
│       ├── ko/                # 한국어 포스트
│       └── en/                # 영어 포스트
├── components/
│   ├── PostCard.tsx           # 포스트 카드 (Placeholder 포함)
│   ├── MDXContent.tsx         # MDX 렌더링
│   ├── ShareButtons.tsx       # 공유 버튼
│   └── NewsletterForm.tsx     # 뉴스레터 구독
├── lib/
│   ├── posts.ts               # 포스트 데이터 로딩
│   ├── tag-utils.ts           # 태그 색상/아이콘
│   └── i18n.ts                # 다국어 설정
└── public/
    └── images/
        └── posts/             # 커버 이미지
```

## Work Queue System

작업 상태 관리를 위한 중앙 큐 시스템입니다.

```
scripts/lib/work-queue.ts
└─ data/work-queue.json
```

**상태:**
- `claimed`: 작업 중 (24시간 후 자동 해제)
- `completed`: 완료됨 (slug 포함)

**자동 정리:**
- 24시간 이상 claimed 상태 → 자동 해제
- 중복 작업 방지
- 긴급 복구용 `forceReleaseAllClaims()` 제공

## Data Flow

### Complete Pipeline

```
┌───────────────────────────────────────────────────────────────────────────┐
│                           AUTOMATED PIPELINE                               │
├───────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  DC Inside Gallery                                                         │
│        │                                                                   │
│        ▼ (5 pages, 1s delay)                                               │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  data/raw/*.json (440+ posts)                                     │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│        │                                                                   │
│        ▼ (quality score ≥ 30, 500자+, max 3/run)                            │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  data/selected/*.json + work-queue.claimed                        │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│        │                                                                   │
│        ▼ (Gemini + Google Search, 3 retries)                               │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  data/verified/*.json (verificationScore ≥ 0.6)                   │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│        │                                                                   │
│        ▼ (Gemini translation)                                              │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  + translation { title_en, content_en, slug }                     │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│        │                                                                   │
│        ▼ (MDX generation)                                                  │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  apps/web/content/posts/ko/{slug}.mdx                             │    │
│  │  apps/web/content/posts/en/{slug}.mdx                             │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│        │                                                                   │
│        ▼ (Gemini image generation, 3 retries)                              │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  apps/web/public/images/posts/{slug}.{ext}                        │    │
│  │  + MDX frontmatter: coverImage: "/images/posts/{slug}.{ext}"      │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│        │                                                                   │
│        ▼ (Next.js build)                                                   │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  .next/ (Static Site Generation)                                  │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│        │                                                                   │
│        ▼ (git commit + push)                                               │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  Vercel Auto-Deploy                                               │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                            │
└───────────────────────────────────────────────────────────────────────────┘
```

## GitHub Actions Workflow

`.github/workflows/auto-update.yml`

```yaml
Schedule: 하루 4회
├─ 17:00 UTC (02:00 KST)
├─ 23:00 UTC (08:00 KST)
├─ 05:00 UTC (14:00 KST)
└─ 11:00 UTC (20:00 KST)

Steps:
1. Checkout + Setup (pnpm, Node.js 20)
2. Cleanup stale claims (24h+)
3. Crawl (5 pages)
4. Auto-select (score ≥ 30, max 3, 500자+)
5. Verify (3 retries) - 실패 시 파이프라인 중단
6. Translate (3 retries) - 실패 시 파이프라인 중단
7. Generate MDX - 이중 검증 (원본 + 구조화 제목)
8. Generate Images (3 retries)
9. Build
10. Commit + Push
11. Report status
```

## Technology Stack

| Component | Technology | Reason |
|-----------|------------|--------|
| Framework | Next.js 14 (App Router) | SSG, i18n, Vercel 통합 |
| Content | MDX (next-mdx-remote) | Markdown + React |
| Styling | Tailwind CSS | 빠른 스타일링 |
| i18n | next-intl | 다국어 지원 |
| Crawling | Cheerio (axios) | 빠른 HTML 파싱 |
| AI | Gemini API | 검증/번역/이미지 |
| Image Gen | gemini-3-pro-image-preview | AI 이미지 생성 |
| Deployment | Vercel | 자동 배포, Edge CDN |
| Automation | GitHub Actions | Cron 스케줄, CI/CD |
| Queue | work-queue.json | 상태 관리 |

## Error Handling

### Retry Strategy

| 단계 | 최대 재시도 | 대기 시간 |
|------|------------|----------|
| Verify | 3회 | 10초 |
| Translate | 3회 | 10초 |
| Image Gen | 3회 | 15초 |

### Auto-Recovery

- **work-queue 타임아웃**: 24시간 이상 claimed → 자동 해제
- **크롤링 실패 허용**: 기존 데이터로 진행 (유일한 continue-on-error)
- **검증/번역/MDX 실패 시 중단**: 불완전한 데이터 커밋 방지
- **빌드 필수**: 빌드 실패 시 push 차단

## Security

- API 키: GitHub Secrets로 관리
- Rate limiting: 크롤링 1초 딜레이
- No sensitive data: 사용자 데이터 미저장
- CORS: API 라우트 설정

## Performance

- SSG: 정적 사이트 생성
- ISR: 증분 정적 재생성
- Image: Next.js Image 최적화
- CDN: Vercel Edge Network
- Caching: 브라우저/CDN 캐싱

## Monitoring

**Pipeline Status Report:**
```
📊 Pipeline Summary
====================
- Raw posts: 440
- Selected: 64
- Verified: 30
- Published (ko): 80
- Published (en): 50
- Images: 89
```

## File Locations

| 항목 | 경로 |
|------|------|
| 원본 데이터 | `data/raw/*.json` |
| 선별된 데이터 | `data/selected/*.json` |
| 검증된 데이터 | `data/verified/*.json` |
| 한국어 포스트 | `apps/web/content/posts/ko/*.mdx` |
| 영어 포스트 | `apps/web/content/posts/en/*.mdx` |
| 커버 이미지 | `apps/web/public/images/posts/*` |
| 작업 큐 | `data/work-queue.json` |
| GitHub Actions | `.github/workflows/auto-update.yml` |
